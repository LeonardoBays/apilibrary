spring:
  application:
    name: library
  datasource:
    url: jdbc:postgresql://localhost:5432/library_db
    username: postgres
    password: 123456
    driver-class-name: org.postgresql.Driver
  jpa:
    hibernate:
      ddl-auto: update
    properties:
      hibernate:
        dialect: org.hibernate.dialect.PostgreSQLDialect
        format_sql: true
    show-sql: true

server:
  port: 8080

jwt:
  # This is a Base64-encoded 512-bit (64-byte) secret key. Generate your own with:
  # openssl rand -base64 64
  # For production, use environment variables instead of hardcoding
  secret: n5r8q2mP9xK3vL7dJ5gB8wS1tU4yH6cF0eA2bM4nO5pR7sT9uV1wX3yZ5aCdEfGhIjKlMnOpQrStUvWxYzAbCdEfGhIjKl
  expiration: 86400000

